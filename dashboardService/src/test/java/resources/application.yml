spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/posts
    username: root
    password: 2589
    driver-class-name: org.mariadb.jdbc.Driver

  #Hibernate settings to control ORM
  jpa:
    show-sql: true #log sql generated by hibernate ofr debug
    hibernate:  #controls automatic generation and update schema based on mappings
      ddl-auto: update